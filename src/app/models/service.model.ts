export interface ServiceCard {
  title: string;
  description: string;
  icon: string;
  bgIcon: string;
  gap: string;
}

export interface Steps {
  title: string;
  description: string;
  number: number;
  cta: any;
}
